/*
 * Please notice that even though, for the execution
 * of the example code at main, it is not necessary 
 * to declare the var "variable" as "extern",
 * if we want to be able to test it's value, we need
 * to declare it as extern.
 */
extern char *variable;

int add_numbers(int x, int y);
char *give_me_a_string();
void mod_var_value(char *value);
void print_variable();

